/* This class should create a Flag object, which will output
* an array of timestamps. This class will also contain methods that the Cutter file can then use to
* move to the outputted Flag.
*
* see http://www.labbookpages.co.uk/audio/javaWavFiles.html for extra data and research materials
*
*/
public class Flag {
  private float time;
  WavFile w;
  // Here's where the heavy lifting needs to go.
  public Flag(String fileName) {
    w = new WavFile(fileName);
    w.
    float totalTime = c.getTimeEl();

  }
  public float getTime() {
    return time;
  }
}
